// <auto-generated>
// DO NOT CHANGE THIS FILE.
// </auto-generated>
using AtCoder.Internal;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.InteropServices;

namespace Kzrnm.Competitive
{
    partial class Xoshiro256
    {
        /// <summary>
        /// 英小文字からなる長さ <paramref name="length"/> の <see cref="string"/> を返します。
        /// </summary>
        public string NextString(int length)
        {
            var arr = new char[length];
            for (int i = 0; i < arr.Length; i++)
                arr[i] = (char)(NextInt32(26) + 'a');
            return new string(arr);
        }

        /// <summary>
        /// [<paramref name="minValue"/>, <paramref name="maxValue"/>) かつ a &lt; b を満たす値 a, b を返します。
        /// </summary>
        public (int, int) Choice2(int minValue, int maxValue)
        {
            Contract.Assert(maxValue - minValue > 1);
            int a, b;
            do
            {
                a = NextInt32(minValue, maxValue);
                b = NextInt32(minValue, maxValue);
            } while (a == b);
            if (a > b) (a, b) = (b, a);
            return (a, b);
        }

        /// <summary>
        /// [<paramref name="minValue"/>, <paramref name="maxValue"/>) の範囲から <paramref name="n"/> 個を選んで返します。
        /// </summary>
        public int[] Choice(int n, int minValue, int maxValue)
        {
            var r = maxValue - minValue;
            Contract.Assert(n <= r);
            var list = new List<int>(Enumerable.Range(minValue, r));
            Shuffle(list.AsSpan());
            var span = list.AsSpan()[..n];
            span.Sort();
            return span.ToArray();
        }

        /// <summary>
        /// 長さ <paramref name="length"/> の配列を返します。
        /// </summary>
        public int[] NextIntArray(int length)
        {
            var arr = new int[length];
            for (int i = 0; i < arr.Length; i++)
                arr[i] = NextInt32();
            return arr;
        }
        /// <summary>
        /// [<paramref name="minValue"/>, <paramref name="maxValue"/>) の値からなる長さ <paramref name="length"/> の配列を返します。
        /// </summary>
        public int[] NextIntArray(int length, int minValue, int maxValue)
        {
            var arr = new int[length];
            for (int i = 0; i < arr.Length; i++)
                arr[i] = NextInt32(minValue, maxValue);
            return arr;
        }
    }
}
