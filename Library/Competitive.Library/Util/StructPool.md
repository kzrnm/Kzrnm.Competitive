---
title: 構造体の一括確保
documentation_of: ./StructPool.cs
---

## 概要

`class` インスタンスを毎回 `new Class()` で作成しているとヒープの確保が細々と行われるので遅い。一方で、平衡二分木のように `null` を扱いたい場合は `struct` にできないので渋々 `class` にせざるをえない。

参照カウンタ式のガベコレを簡易実装することで構造体をヒープのように扱えるようにしている。